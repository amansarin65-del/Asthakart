import './globals.css';

export const metadata = { title: 'AasthaKart — Faith, together', description: 'A modern marketplace for worship and spiritual products across traditions.' };

export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
